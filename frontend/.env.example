# Environment Configuration
# Options: development, production
NODE_ENV=development

# API Configuration (used in production)
API_URL=https://productiondomain.com/api/v1/generate_cover_letter

# When NODE_ENV=development, the app will use http://localhost:8000 regardless of API_URL
# When NODE_ENV=production, the app will use the API_URL value above 